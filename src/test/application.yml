spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/market
    username: postgres
    password: hjvfirf18
    driverClassName: org.postgresql.Driver
server:
  port: 8081
